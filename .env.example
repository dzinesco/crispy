# Public site URL — used for SEO and Stripe redirect targets
VITE_SITE_URL=https://crispygoat.com

# API origin for the browser (leave empty in dev to use Vite proxy at /api)
VITE_API_ORIGIN=

# Server (server/.env) — see server/.env.example for the full list
SERVER_PORT=3102
